#pragma once

void movePlayer(char input);
void attack();     // melee: hits whatever's directly in front of the player
void fireArrow();  // ranged: spawns a bullet traveling in the facing direction
void heal();
